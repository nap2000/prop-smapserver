/*
 * Set the idb version consistently in webforms and mywork
 * This is a temporary solution which will be replaced when the build for each of these components is aligned
 */

window.idbConfig = {
	version: 15
};
